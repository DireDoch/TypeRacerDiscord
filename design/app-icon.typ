// Issue #112 — icône d'application Discord finale.
// Uniquement le wordmark logo() ("Typpe|Racer") — pas de scène voiture,
// changement définitif.
#import "components.typ": palette, logo

#set page(width: 10cm, height: 10cm, margin: 0cm, fill: palette.bg)

#align(center + horizon)[#logo(size: 1.4cm)]
